package manuelklyukvin.contacts_app.build_src.configs

object GradleNamespaces {
    private const val BASE = "manuelklyukvin.contacts_app"

    const val APP = "$BASE.app"
    const val CORE = "$BASE.core"

    const val MAIN = "$BASE.main"
}